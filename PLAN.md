# ChainLens — Web3 项目检查

> **一句话**：输入项目名、官网或合约地址，ChainLens 帮普通人看清项目方说了什么、哪些能查到证据、哪些还只是宣传。

---

## 📌 赛道推荐

> [!IMPORTANT]
> **推荐报名「Public Goods / 公共物品」赛道**
>
> 理由：
> 1. **完美契合**：风险检测工具本质是为整个 Web3 生态提供公共服务，保护用户资产安全
> 2. **评委偏好**：Public Goods 赛道看重「对生态的长期价值」，而非短期商业变现 — 安全工具天然符合
> 3. **竞争优势**：大部分 Public Goods 项目做的是 infra/dev tools，做消费者保护工具会很出挑
> 4. **表达加分**：说清楚“帮普通人少被 Web3 宣传绕进去”，比“又一个 DeFi 协议”更容易打动评委
>
> **备选**：如果赛道设有「AI × Crypto」，也可以报（AI 辅助分析是加分项）

---

## 📌 产品形态

> [!TIP]
> **Web App**（纯网页应用）
> 1. **Demo 效果最好**：评委面前直接打开网页，输入地址，实时出报告
> 2. **开发速度最快**：48 小时内集中火力打磨核心功能和 UI
> 3. **部署简单**：Vercel 一键部署，评委扫码就能体验

---

## 📌 MVP 范围定义

> [!IMPORTANT]
> **48 小时内只做这 4 件事，做深做透。**

### ✅ MVP 核心（必须完成）

| 模块 | 数据来源 | 输出 |
|------|----------|------|
| 🔒 **合约安全分析** | GoPlus Token Security API | 是否开源、是否蜜罐、mint/pause/blacklist 权限、代理合约、Owner 权限 |
| 📊 **持仓集中度分析** | GoPlus `holders` 字段 | Top 10 持仓占比、巨鲸钱包标记、集中度风险信号 |
| 💧 **DEX 流动性分析** | GoPlus `lp_holders` + DEXScreener API | 流动性池 USD 价值、LP 持有者集中度、是否发送到黑洞地址（≈锁定） |
| 🎯 **综合风险报告** | 信号聚合引擎 | 风险信号列表 + 置信度 + 雷达图 + 证据链 |

### 🔮 加分项（有时间再做 / 演示模式）

| 模块 | 说明 | 优先级 |
|------|------|--------|
| 🤖 AI 白皮书分析 | 用户粘贴白皮书 URL，LLM 分析技术可行性 | P2 |
| 📱 社交媒体分析 | 推特粉丝真实度、互动率 | P3（API 成本高，跳过） |
| 📈 链上活跃度 | 日活地址趋势、刷量检测 | P3（Etherscan 免费 tier 数据有限） |

---

## 📌 数据源可行性核实

> [!NOTE]
> 以下是经过实际验证的 API 能力边界，避免开发时踩坑。

### GoPlus Token Security API ✅ 主力数据源

- **端点**：`GET https://api.gopluslabs.io/api/v1/token_security/{chain_id}?contract_addresses={address}`
- **免费额度**：100 Call Credits/分钟（非按月计费），无需 API Key 即可调用
- **⚠️ 限流应对**：后端必须加请求队列（p-queue, max 1 req/sec）+ 指数退避重试；Demo 强依赖 fixture 缓存，实时请求仅作补充
- **实际返回字段**（与 MVP 直接相关）：

| 字段 | 含义 | MVP 用途 |
|------|------|----------|
| `is_open_source` | 合约是否开源已验证 | 合约安全 |
| `is_proxy` | 是否为代理合约 | 合约安全（风险信号，非直接扣分） |
| `is_mintable` | 是否可增发 | 合约安全 |
| `can_take_back_ownership` | Owner 是否可收回权限 | 合约安全 |
| `owner_change_balance` | Owner 能否修改余额 | 合约安全 |
| `hidden_owner` | 是否有隐藏 Owner | 合约安全 |
| `selfdestruct` | 是否可自毁 | 合约安全 |
| `is_honeypot` | 是否为蜜罐 | 合约安全（关键红旗） |
| `buy_tax` / `sell_tax` | 买卖税率 | 合约安全 |
| `is_blacklisted` | 是否有黑名单功能 | 合约安全 |
| `is_whitelisted` | 是否有白名单功能 | 合约安全 |

> [!WARNING]
> **三值逻辑（P1 修复）**：GoPlus 返回的布尔字段实际有三种状态：`"1"` (true) / `"0"` (false) / `null`/缺失 (unknown)。
> 信号引擎必须区分 `false`（确认没有该功能）和 `unknown`（API 未返回/无法判断）。
> - `false` → 不触发信号
> - `true` → 触发对应信号
> - `null`/缺失 → 触发 `low` severity 信号：「该字段数据缺失，无法判断」
>
> **实测注意**：USDC 等大型合规代币在 GoPlus 中部分权限字段可能返回 `null` 而非 `"1"`。开发第一步应先对 5 个 fixture 地址实际调用 GoPlus，dump 原始 JSON，以真实响应为准设计信号规则。
| `holder_count` | 持有者总数 | 持仓分析 |
| `holders` | Top 持仓者列表（地址 + 占比） | 持仓集中度 |
| `lp_holder_count` | LP 持有者数量 | 流动性分析 |
| `lp_holders` | LP 持仓者列表（含是否锁定标记） | 流动性锁定判断 |
| `lp_total_supply` | LP 总供应量 | 流动性分析 |
| `token_name` / `token_symbol` | 代币名称 | 基础信息 |
| `total_supply` | 总供应量 | 基础信息 |

> [!TIP]
> **关键发现**：GoPlus 的 `lp_holders` 字段会标记 LP 是否发送到了黑洞地址（`0x000...dead`），这等价于「流动性是否锁定/销毁」。不需要依赖 DEXScreener 判断锁定状态。

### DEXScreener API ✅ 补充数据源

- **端点（推荐 v1）**：
  - 按链+地址查代币 pairs：`GET https://api.dexscreener.com/token-pairs/v1/{chainId}/{tokenAddress}`
  - 批量查代币信息：`GET https://api.dexscreener.com/tokens/v1/{chainId}/{tokenAddresses}`（最多 30 个地址，逗号分隔）
  - 旧端点 `/latest/dex/tokens/{address}` 仍可用，但会混入跨链/多 pair 结果
- **免费额度**：300 请求/分钟，无需 API Key
- **主 pair 选择逻辑**：按所选链过滤 → 只取 `baseToken.address` 匹配的 pair → 按 `liquidity.usd` 降序取第一个
- **实际返回字段**：

| 字段 | 含义 | MVP 用途 |
|------|------|----------|
| `liquidity.usd` | 流动性池 USD 价值 | 流动性规模判断 |
| `priceUsd` | 当前价格 | 报告展示 |
| `priceChange.h24` | 24h 价格变化 | 报告展示 |
| `volume.h24` | 24h 交易量 | 报告展示 |
| `fdv` | 完全稀释估值 | 报告展示 |
| `pairAddress` | 交易对地址 | 关联信息 |
| `dexId` | DEX 平台 | 关联信息 |
| `txns` | 交易笔数 | 活跃度参考 |

> [!WARNING]
> DEXScreener **不提供** LP 锁定状态、锁定时长等字段。这些信息从 GoPlus `lp_holders` 获取。

### Etherscan API ⚠️ 辅助用途

- **免费 tier 限制**：3 调用/秒，100K 调用/天
- **Token Holder List 是 PRO 付费功能**，免费 tier 不可用
- **MVP 中仅用于**：合约源码验证状态（`getabi`）、基础合约信息（作为 GoPlus 的备用/交叉验证）
- **结论**：持仓数据完全依赖 GoPlus，不依赖 Etherscan

---

## 📌 风险评估模型

> [!IMPORTANT]
> **核心设计原则**：输出「风险信号 + 证据 + 置信度」，而不是单一结论分。
> 用户看到的是「这些信号需要你注意」，而不是「这个项目是骗局」。

### 信号模型（替代硬评分）

每个检测维度输出一组**风险信号**，每个信号包含：

```json
{
  "signal": "合约存在 mint 增发权限",
  "severity": "medium",
  "confidence": 0.95,
  "evidence": "GoPlus is_mintable = true",
  "context": "部分正规项目（如治理代币）也会保留 mint 权限用于通胀激励，需结合其他信号综合判断"
}
```

### 三大维度 → 信号输出

#### 维度 1：合约安全信号

| 检测项 | severity | 说明 |
|--------|----------|------|
| 合约未开源 | 🔴 high | 无法审查代码，风险不可控 |
| 蜜罐（is_honeypot） | ⛔ critical | 用户无法卖出，几乎确认恶意 |
| 可增发（is_mintable） | 🟡 medium | 正规项目也可能有，需看上下文 |
| 有黑名单功能 | 🟡 medium | 部分合规代币使用（如 USDC） |
| 有暂停功能 | 🟡 medium | 紧急暂停是安全实践，但也可被滥用 |
| 隐藏 Owner | 🔴 high | Owner 权限不透明，风险较高 |
| Owner 可修改余额 | ⛔ critical | 极危险，可直接操纵代币 |
| 高买卖税 (>10%) | 🔴 high | 异常高税率，疑似收割 |
| 合约可自毁 | 🔴 high | 可直接销毁合约跑路 |
| 代理合约 | 🟢 low | 正规项目常用升级模式，但增加了不透明性 |

#### 维度 2：持仓集中度信号

| 检测项 | severity | 说明 |
|--------|----------|------|
| Top 10 持仓 > 80% | ⛔ critical | 极度集中，少数地址控盘 |
| Top 10 持仓 > 50% | 🔴 high | 集中度偏高 |
| Top 10 持仓 > 30% | 🟡 medium | 中等集中（需排除合约/交易所地址） |
| 总持有者 < 100 | 🔴 high | 持有者极少，流通性差 |
| 总持有者 < 500 | 🟡 medium | 持有者较少 |

> [!NOTE]
> **关键设计**：GoPlus 返回的 `holders` 列表会包含 DEX 合约地址、跨链桥地址等。评分时需要标记这些已知合约地址（如 Uniswap Router），将它们与「个人巨鲸钱包」区分开来，避免误伤。

#### 维度 3：流动性信号

| 检测项 | severity | 说明 |
|--------|----------|------|
| 流动性 < $1K | ⛔ critical | 几乎无流动性，极易操纵 |
| 流动性 < $10K | 🔴 high | 流动性很低 |
| 流动性 < $100K | 🟡 medium | 流动性一般 |
| LP 未锁定/未销毁 | 🔴 high | 开发者可随时撤走流动性 |
| LP 集中在单一地址 | 🟡 medium | 流动性由少数人控制 |

### 综合风险等级

基于信号聚合，输出风险等级：

| 等级 | 触发条件 | 标签（前端展示） |
|------|----------|-----------------|
| 🟢 **低风险** | 0 个 critical + 0 个 high | 未发现明显风险信号 |
| 🟡 **需关注** | 0 个 critical + 1-2 个 high | 存在风险信号，建议进一步研究 |
| 🟠 **较高风险** | 0 个 critical + ≥3 个 high，或 1 个 critical | 多个风险信号，需谨慎评估 |
| 🔴 **高风险** | ≥2 个 critical | 大量高危信号，强烈建议人工复核 |

> [!WARNING]
> **措辞规范**：前端展示中不使用「骗局」「传销」「确定是诈骗」等定性措辞。
> 统一使用：「风险信号」「需要关注」「建议人工复核」「数据异常」。
> 这既更专业，也避免误判引发的争议。

---

## 📌 技术架构

```
┌───────────────────────────────────────────────────┐
│                前端 (Vite + Vanilla JS)             │
│  ┌──────────┐ ┌────────────┐ ┌────────────────┐   │
│  │ 搜索输入  │ │ 雷达图(3维) │ │ 风险信号卡片    │   │
│  │ + 链选择  │ │ (Chart.js) │ │ + 证据 + 上下文 │   │
│  └──────────┘ └────────────┘ └────────────────┘   │
│  ┌──────────────────────────────────────────────┐  │
│  │  详细分析展开（合约 / 持仓 / 流动性）          │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────┬─────────────────────────────┘
                      │ REST API
┌─────────────────────▼─────────────────────────────┐
│               后端 (Node.js + Express)              │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │  API 聚合层                                   │  │
│  │  ┌────────┐  ┌────────────┐  ┌──────────┐   │  │
│  │  │ GoPlus │  │DEXScreener │  │ Etherscan│   │  │
│  │  │(主力)  │  │ (补充)     │  │ (备用)   │   │  │
│  │  └────────┘  └────────────┘  └──────────┘   │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │  风险信号引擎                                  │  │
│  │  合约信号 → 持仓信号 → 流动性信号 → 聚合       │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │  缓存层 (内存缓存 + Demo fixture)             │  │
│  │  - 预缓存 5 个测试地址的完整结果               │  │
│  │  - 实时查询结果缓存 10 分钟                    │  │
│  └──────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```

### 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Vite + Vanilla JS + CSS | 快速构建，无框架负担 |
| 图表 | Chart.js | 雷达图 + 信号仪表盘 |
| 后端 | Node.js + Express | API 聚合 + 信号计算 + 缓存 |
| 主数据源 | GoPlus Security API | 合约安全 + 持仓 + LP 数据 |
| 补充数据源 | DEXScreener API | 流动性 USD + 价格 + 交易量 |
| 备用数据源 | Etherscan API | 合约验证状态（交叉校验） |
| 部署 | Vercel (前端) + Render (后端) | 免费部署 |

---

## 📌 UI 设计方向

### 设计风格
- **暗色系 + 赛博朋克**：深色背景 `#0a0e1a` + 霓虹蓝 `#00d4ff` + 警告红 `#ff3b5c` + 安全绿 `#00ff88`
- **Glassmorphism 卡片**：半透明毛玻璃风险信号卡片
- **微动画**：信号加载动画、雷达图绘制动画、风险标签弹入效果

### 页面结构

```
┌──────────────────────────────────────────────────────┐
│  🔍 ChainLens                         [ETH ▾]        │  ← 顶栏 + 链选择
├──────────────────────────────────────────────────────┤
│                                                       │
│   ╔════════════════════════════════════════════╗      │
│   ║  输入 ERC-20 合约地址                       ║      │  ← 搜索区
│   ║  [0x1f9840a85...]              [🔍 分析]   ║      │
│   ╚════════════════════════════════════════════╝      │
│                                                       │
│   ┌────────────┐  ┌──────────────────────────┐       │
│   │ 风险等级    │  │                          │       │
│   │  🟡 需关注  │  │   雷达图 (3 维度)         │       │  ← 概览
│   │            │  │   合约安全 / 持仓 / 流动性 │       │
│   │ UNI Token  │  │                          │       │
│   │ $7.23      │  │                          │       │
│   └────────────┘  └──────────────────────────┘       │
│                                                       │
│   ⚠️ 风险信号（3 个）                                  │
│   ┌──────────────────────────────────────────┐       │
│   │ 🟡 MEDIUM │ 合约存在 mint 增发权限         │       │
│   │   证据: GoPlus is_mintable = true         │       │  ← 信号列表
│   │   上下文: 治理代币常保留此权限...           │       │
│   ├──────────────────────────────────────────┤       │
│   │ 🟡 MEDIUM │ 代理合约 (Proxy)              │       │
│   │   ...                                    │       │
│   └──────────────────────────────────────────┘       │
│                                                       │
│   📊 详细数据                                         │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│   │ 合约安全  │ │ 持仓分布  │ │ 流动性   │            │  ← 3 个展开卡片
│   │ 10项检查  │ │ Top10图表 │ │ LP 分析  │            │
│   └──────────┘ └──────────┘ └──────────┘            │
│                                                       │
│   ⚖️ 免责声明: 本工具仅提供链上数据的客观呈现，         │
│   不构成投资建议。请结合多方信息自行判断。              │  ← 免责
└──────────────────────────────────────────────────────┘
```

---

## 📌 项目结构

```
黑客松/
├── client/                        # 前端 (Vite)
│   ├── index.html
│   ├── src/
│   │   ├── main.js                # 入口 + 事件绑定
│   │   ├── api.js                 # 与后端通信
│   │   ├── charts.js              # Chart.js 雷达图
│   │   ├── render.js              # DOM 渲染（信号卡片、详情）
│   │   └── utils.js               # 工具函数
│   ├── styles/
│   │   ├── index.css              # 设计系统 + 主样式
│   │   └── animations.css         # 动画
│   ├── public/
│   │   └── favicon.svg
│   └── vite.config.js
│
├── server/                        # 后端 (Node.js)
│   ├── index.js                   # Express 入口 + CORS
│   ├── routes/
│   │   └── analyze.js             # POST /api/analyze
│   ├── services/
│   │   ├── goplus.js              # GoPlus API 封装
│   │   ├── dexscreener.js         # DEXScreener API 封装
│   │   └── signals.js             # 风险信号引擎
│   ├── fixtures/                  # Demo 预缓存数据（含 chainId）
│   │   ├── uni.json               # UNI 缓存结果
│   │   ├── usdc.json              # USDC 缓存结果
│   │   ├── aave.json              # AAVE 缓存结果
│   │   ├── pepe.json              # PEPE 缓存结果
│   │   └── high_risk.json         # 高风险样本（开发时实测确认）
│   ├── cache.js                   # 内存缓存（TTL 10min）
│   ├── rate-limiter.js            # GoPlus 请求队列（p-queue, 1 req/sec）
│   ├── package.json
│   └── .env.example               # API Key 模板
│
├── PLAN.md                        # 本文件
└── README.md
```

---

## 📌 Verification Plan

### 固定测试地址 (Fixture)

> [!IMPORTANT]
> **Fixture 结构必须包含 chainId**，格式为 `{ chainId, chainName, address, label }`。
> Demo 前必须对以下地址跑通完整流程，缓存结果作为 fixture。
> 现场如果 API 崩了/限流了，自动 fallback 到缓存数据。

> [!CAUTION]
> **P0 修复：SQUID 鱿鱼币实际在 BSC (chain_id=56)，不在 ETH 主网。**
> GoPlus ETH (chain_id=1) 查 SQUID 地址返回空结果，Demo 会直接失败。
>
> **解决方案**：高风险样本不硬编码，改为开发启动第一步（Day 1 第 2-3h）实测确认：
> 1. 用 GoPlus API 扫一批已知问题代币（从 TokenSniffer 低分列表或 Chainabuse 获取 ETH 地址）
> 2. 筛选出在 ETH 主网上 GoPlus 确实返回 `is_honeypot=1` 或多个高危字段的地址
> 3. 确认后写入 `fixtures/high_risk.json`
> 4. 如果 ETH 上实在找不到好样本，则 MVP 加入 BSC 支持（GoPlus chain_id=56，仅此一个额外链）

| 代币 | 链 | 合约地址 | 预期风险等级 | 测试目的 |
|------|-----|---------|------------|--------|
| **UNI** | ETH (1) | `0x1f9840a85d5aF5bf1D1762F925BDADdC4201F984` | 🟢/🟡 | 正规项目，可能有 mint（治理需要） |
| **USDC** | ETH (1) | `0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48` | 🟢/🟡 | 合规稳定币 → 验证不误伤（注意字段可能为 null） |
| **AAVE** | ETH (1) | `0x7Fc66500c84A76Ad7e9c93437bFc5Ac33E2DDaE9` | 🟢 | 正规 DeFi 协议 |
| **PEPE** | ETH (1) | `0x6982508145454Ce325dDbE47a25d4ec3d2311933` | 🟡 | Meme 币，持仓可能集中 |
| **待定** | ETH (1) 或 BSC (56) | 开发时实测确认 | 🔴 | 高风险样本，需 GoPlus 返回 critical 信号 |

### 缓存与降级策略

```
请求流程:
1. 检查内存缓存 (TTL 10min) → 命中则直接返回
2. 并行调用 GoPlus + DEXScreener API
3. 任一 API 失败 → 检查 fixtures/ 目录是否有预存数据
4. fixture 也没有 → 返回 partial result（标注哪些维度数据缺失）
5. 成功的结果写入内存缓存 + 可选写入 fixture
```

### 开发阶段验证步骤

1. **API 原始数据 dump（第一步！）**：对 5 个 fixture 地址调用 GoPlus API，将原始 JSON 存入 `fixtures/raw/`，逐字段检查哪些是 `"1"`/`"0"`/`null`/缺失
2. **三值逻辑验证**：
   - USDC → 检查 `is_blacklisted` 实际返回值（可能是 null 而非 `"1"`）。如果 null，信号引擎应输出 `low` severity「数据缺失」而非 `medium`「有黑名单」
   - UNI → 不应出现 critical 信号
   - 高风险样本 → 应触发多个 critical/high 信号
3. **前端渲染验证**：5 个地址的报告页面截图存档，确认 UI 正确展示所有信号（包括 unknown 状态的灰色展示）
4. **降级验证**：断网后重启服务器，确认 fixture fallback 正常工作
5. **限流验证**：连续快速提交 5 个不同地址，确认请求队列有序、不触发 GoPlus 429

### Demo 演示流程（建议）

```
1. 先展示高风险样本 → 满屏红色/橙色警告 → 震撼效果
2. 再展示 UNI → 绿色/黄色 → 对比效果明显
3. 展示 USDC → 部分字段标注「数据未返回」→ 体现不误伤 + 三值逻辑的严谨性
4. 邀请评委现场输入任意地址 → 实时分析（有缓存 + 限流队列兜底）
```

> [!TIP]
> 现场 Demo 前 10 分钟，后台先手动触发一次对 5 个 fixture 地址的预热请求，确保缓存已加载。

---

## 📌 48 小时开发路线图

### Day 1（0-12h）：后端核心

| 时段 | 任务 | 产出 |
|------|------|------|
| 0-2h | 项目初始化（Vite + Express + 目录结构） | 前后端跑通 hello world |
| 2-5h | GoPlus API 封装 + DEXScreener API 封装 | 两个 service 可独立调用 |
| 5-8h | 风险信号引擎（signals.js） | 输入 API 原始数据 → 输出信号列表 |
| 8-10h | `/api/analyze` 路由 + 缓存层 | 完整 API 可调用 |
| 10-12h | 5 个 fixture 地址跑通 + 缓存预热 | fixture JSON 文件生成 |

### Day 1（12-24h）：前端骨架

| 时段 | 任务 | 产出 |
|------|------|------|
| 12-15h | CSS 设计系统（色彩、卡片、动画） | 暗色主题 + Glassmorphism 基础 |
| 15-18h | 搜索 + 概览区（风险等级 + 雷达图） | 核心页面上半部分 |
| 18-21h | 信号列表 + 详情卡片 | 核心页面下半部分 |
| 21-24h | 联调 + 真实数据渲染 | 前后端完整跑通 |

### Day 2（24-40h）：打磨

| 时段 | 任务 | 产出 |
|------|------|------|
| 24-28h | 动画效果 + 加载状态 + 错误处理 | 用户体验完善 |
| 28-32h | 降级策略实现 + 边界情况测试 | 稳定性保证 |
| 32-36h | 响应式适配 + 细节打磨 | 移动端可看 |
| 36-40h | 部署 Vercel + Render | 线上可访问 |

### Day 2（40-48h）：Demo 准备

| 时段 | 任务 | 产出 |
|------|------|------|
| 40-44h | 5 个地址完整演示验证 + 截图 | Demo 素材 |
| 44-46h | PPT / 演讲稿准备 | 展示材料 |
| 46-48h | 彩排 + 应急预案 | 上台准备 |

---

## 📌 Open Questions

> [!IMPORTANT]
> **仍需确认：**

1. **链的范围**：MVP 先只做 ETH 主网？（GoPlus 支持多链，扩展成本低，但 Demo 聚焦一条链更清晰）
2. **API Key**：是否已有 Etherscan API Key？（GoPlus 和 DEXScreener 不需要）
3. **团队人数与分工**：前后端是否可以并行开发？
4. **黑客松具体时间**：还剩多少小时？需要据此调整路线图
