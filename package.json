{
  "name": "chainlens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "check": "node --check \"api/[...path].js\" && node --check api/health.js && npm run check --workspace server && npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
